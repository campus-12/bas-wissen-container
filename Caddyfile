:80 {
    handle /api/* {
        reverse_proxy 127.0.0.1:3000
    }

    # Frontend mit SPA fallback
    handle {
        root * /app/web
        try_files {path} /index.html
        file_server
    }
}
